/**
 * Joins truthy class names. Re-exports lib/format's `classNames` (which also accepts
 * `{ name: boolean }` records) so the app has one class-name helper.
 */
export { classNames as cx } from '../../lib/format';
